export const getEstimatedTime = (contentString) => {
  const contentArray = JSON.parse(contentString);

  for(let i=0;i<contentArray.length;i++) {
      
  }

  console.log(contentArray);

  return "7 min"
};
